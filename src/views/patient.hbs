<style>
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin-right: 0px;
        background-color: #f9f9f9;
        padding-top: 0px;
        flex-direction: column;
    }
    .card {
        width: 650px;
        background-color: #fff9c4;
        border: 5px solid black;
        border-radius: 15px;
        padding: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        font-family: "Arial", sans-serif;
    }
    .card .photo {
        width: 182px;
        height: 224px;
        border: 3px solid black;
        background-color: white;
        margin-bottom: 10px;
        float: left;
        margin-right: 20px; /* Adjusted for spacing */
        border-radius: 10px;
        position: relative;
        top: 20px;
    }
    .card .photo img {
        max-width: 100%; /* Ensure image fits within its container */
        max-height: 100%; /* Prevent image from exceeding its container */
        object-fit: cover; /* Adjust image scaling */
    }
    .card .details {
        float: left;
        width: calc(100% - 220px); /* Adjust width to account for photo */
        position: relative;
        left: 0px;
    }
    .card .details p {
        margin: 30px;
        word-wrap: break-word; /* Allows breaking long words */
        overflow-wrap: break-word; /* Ensures long text doesn't overflow */
        overflow: hidden; /* Ensures content stays within the container */
    }
    .card .details p span {
        font-weight: bold;
        text-decoration: underline;
        display: inline-block; /* Prevent inline text overflow */
    }
    .diagnostic {
        width: 650px;
        background-color: #f1753c;
        border: 5px solid black;
        border-radius: 15px;
        padding: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        font-family: "Arial", sans-serif;
        margin-top: 20px;
    }
    .diagnostic .photo {
        width: 182px;
        height: 224px;
        border: 3px solid black;
        background-color: white;
        margin-bottom: 10px;
        float: left;
        margin-right: 20px; /* Adjusted for spacing */
        border-radius: 10px;
        position: relative;
        top: 20px;
    }
    .diagnostic .details {
        float: left;
        width: calc(100% - 220px); /* Adjust width to account for photo */
        position: relative;
        left: 0px;
    }
    .diagnostic .details p {
        margin-top: 130px;
    }
    .diagnostic .details ul {
        margin-top: 10px;
        padding-left: 20px; /* Adds padding to the list items */
        list-style-type: disc; /* Display bullet points for items */
        max-height: 180px; /* Limit the height of the list */
        overflow-y: auto; /* Enable vertical scrolling if content overflows */
        word-wrap: break-word;
        overflow-wrap: break-word;
    }
    .diagnostic .details span {
        font-weight: bold;
        display: inline-block;
        word-wrap: break-word;
        overflow-wrap: break-word;
    }
    .diagnostic .details ul::-webkit-scrollbar {
    width: 8px; /* Width of the scrollbar */
}
.diagnostic .details ul::-webkit-scrollbar-thumb {
    background-color: #007bff; /* Color of the scrollbar thumb */
    border-radius: 4px; /* Rounded corners for the thumb */
    border: 2px solid #f1753c; /* Adds padding around the thumb for a better look */
}
.diagnostic .details ul::-webkit-scrollbar-thumb:hover {
    background-color: #0056b3; /* Darker shade when hovered */
}
.diagnostic .details ul::-webkit-scrollbar-track {
    background-color: #f9f9f9; /* Background color of the scrollbar track */
    border-radius: 4px; /* Rounded corners for the track */
    box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.1); /* Subtle shadow for the track */
}
    .diagnostic .checkmark {
        position: relative;
        bottom: -45px;
        right: -380px;
        width: 40px;
        height: 40px;
        border: 5px solid black;
        border-radius: 4px;
        transform: rotate(45deg);
        border-left: none;
        border-top: none;
        border-radius: 10px;
        cursor: pointer;
    }
    .button-link {
        display: inline-block;
        padding: 10px 20px;
        font-size: 16px;
        color: white;
        background-color: #007bff;
        border: none;
        border-radius: 5px;
        text-decoration: none;
        text-align: center;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }
    .button-link:hover {
        background-color: #0056b3;
    }
</style>

<div class="card">
    <div class="photo"><img src="{{qrCodeImage}}" alt="QR Code"/></div>
    <div class="details">
        <p><span>{{patient.cognom1}} {{patient.cognom2}}, {{patient.name}}</span></p>
        <p><span>Edat:</span> {{patient.edat}}</p>
        <p><span>Condició mèdica:</span> {{patient.enfermetat}}</p>
        <p><span>Estat Actual:</span> {{patient.estatActual}}/6</p>
        <p><span>Metge Encarregat:</span> {{patient.metge}}</p>
        <p><span>Centre de Referència:</span> {{patient.centreSanitari}}</p>
    </div>
</div>

<div class="diagnostic">
    <div class="details" style="width: 100%;">
        <h1>Diagnostic</h1>
        <ul>
            {{#each diagnostic}}
                <li>{{this}}</li>
            {{/each}}
        </ul>
        <a href="/patient/edit/{{patient.id}}" class="button-link">Edita la informació</a>
        <a href="/patient/test/{{patient.id}}" class="button-link">Fes un test</a>
    </div>
</div>

<div class="diagnostic">
    <div class="details" style="width: 100%;">
        <h1>Tractament</h1>
        <p>{{tractament}}</p>
    </div>
</div>
</br>
<a href="/chat/{{patient.id}}" class="button-link">Parla amb l'ajudant de {{patient.metge}}</a>
